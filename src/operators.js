export default {
    unary: {
        '+': Symbol('unary.plus'),
        '-': Symbol('unary.minus')
    },

    binary: {
        '+': Symbol('binary.plus'),
        '-': Symbol('binary.minus')
    }
}